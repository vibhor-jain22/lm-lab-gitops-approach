apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-bookstore-api.fullname" . }}
  labels:
    {{- include "devops-bookstore-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-bookstore-api.selectorLabels" . | nindent 4 }}
